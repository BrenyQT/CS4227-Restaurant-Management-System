'use client'

import {useRouter} from "next/navigation";

export default function ManagePage() {
    const router = useRouter();
    return (
        <div>
            <h1>Hello Staff</h1>

        </div>
    );
}